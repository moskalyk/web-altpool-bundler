# web-altpool-bundler
use folders `fluence` and `src`

## steps to run

# terminal #1: run bundler, which keeps track of peers
$ cd fluence/test
$ ts-node test.bundler.ts

# terminal #2: run frontend, which contains an altpool of UserOperations
$ yarn start

#### notes
one way functions are cool, have your influence tho